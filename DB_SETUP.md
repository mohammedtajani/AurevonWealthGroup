Error
localhost:3006# Database Setup

If you cannot run Docker, you must install PostgreSQL manually and update the `DATABASE_URL` in `.env.local` with your local credentials.

Current config assumes:
- User: aurevon_user
- Pass: secure_password_123
- DB: aurevon_db
- Port: 5432
